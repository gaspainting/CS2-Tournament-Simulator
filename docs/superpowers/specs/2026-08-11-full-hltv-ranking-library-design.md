# HLTV 完整排名职业库设计

## 背景

当前应用只内置 16 支职业队和 80 名选手。数据库与导出文件均直接继承这份固定种子，因此“职业名单”不是 HLTV 的完整职业排名库。现有 Tauri 更新器使用无浏览器 HTTP 请求访问 HLTV，当前会被 Cloudflare 的 JavaScript/Cookie 验证页拦截，无法补齐数据库。

本次更新使用 HLTV 2026-08-10 世界排名页作为队伍与当前登记阵容的权威快照，并使用 2026-02-11 至 2026-08-11 的 Players Stats 表补充 Rating。排名页当前包含 215 支队伍和 1,032 名唯一登记选手。其中 180 支队伍拥有完整五人阵容，35 支队伍阵容不足五人，合计缺少 43 个位置。

## 数据范围

- 收录 HLTV 2026-08-10 世界排名页中的全部 215 支队伍，不截断为 Top 30 或 Top 50。
- 每支队伍使用排名卡片中实际存在 HLTV 玩家链接的当前登记选手，不补写缺失位置。
- 选手内部 ID 使用 `hltv-player-<HLTV ID>`，队伍内部 ID 使用 `hltv-team-<HLTV ID>`。
- 游戏 ID、真实姓名和国籍来自排名页的选手链接与图片属性。
- 真实姓名去除显示文本中的单引号游戏 ID；无法可靠拆分时保留 HLTV 原始显示名。
- Rating 按 HLTV 玩家 ID 从近六个月 Stats 表匹配；无样本时使用 `1.00`，并将 `sampleStatus` 标为 `insufficient`。
- 年龄无法从排名页或 Stats 表稳定获得，统一使用现有兼容默认值 `24`。
- 位置无法从这两个来源稳定获得，统一使用 `Unset`，不伪造 IGL、AWPer 等角色。
- 队伍地区使用 `International`，颜色按 HLTV 队伍 ID 确定性生成，稳定度使用现有职业更新默认值 `0.72`。
- 队伍评分使用 `round(1000 + 当前登记成员平均 Rating * 600)`。

## 数据文件与运行时结构

新增 `src/data/hltvProfessionalSnapshot.json` 保存可审计的结构化快照。`src/data/proTeams.ts` 不再维护超长手写数组，而是验证并映射 JSON，导出既有的 `PROFESSIONAL_PLAYERS`、`PROFESSIONAL_TEAMS` 和 `PROFESSIONAL_SNAPSHOT_DATE`，从而保持调用方接口不变。

快照包含：

- 数据日期与两个 HLTV 来源 URL；
- 215 支队伍的排名、HLTV ID、名称和当前登记阵容；
- 1,032 名唯一选手的 HLTV ID、游戏 ID、真实姓名、国籍和 Rating；
- 完整队伍数 `180`、不完整队伍数 `35` 和缺失位置数 `43`；
- 未匹配到 Stats 样本的选手数量，便于后续审计。

生成快照时必须拒绝：

- 重复队伍 ID；
- 重复选手 ID；
- 同一选手出现在两支排名队伍；
- 阵容为空、超过五人或包含重复选手的队伍；
- 队伍引用不存在的选手；
- 空白名称、无效 Rating 或非法 HLTV ID。

职业选手的官方游戏 ID 不做虚构式改名。若 HLTV 官方存在同名选手，内部 ID 仍保持唯一，导出中保留官方游戏 ID。

职业队允许在队伍库中保存少于五名首发的当前阵容。现有 `validateRoster` 仍将这些队伍判定为不可参赛，因此创建赛事、自动补齐参赛名单和操纵队选择会自动排除 35 支不完整队伍。虚构队和自建队继续要求完整五人阵容；不放宽它们的校验规则。

## 现有数据库迁移

在 `AppDatabase.migration` 中新增 `professionalSnapshotVersion`，完整排名快照版本为 `2`。

新数据库直接写入版本 2，并使用 215 队、1,032 人职业库。旧数据库加载时：

1. 若迁移版本已经达到 2，不重复处理。
2. 若职业快照仍精确匹配旧内置基线（来源日期 `2026-08-10`、16 队、80 人），用完整排名职业队与选手替换当前职业库。
3. 若用户已经通过导入、在线更新或手工维护得到不同职业库，不覆盖其职业数据，只记录迁移版本。
4. 虚构队、自建队、模板、设置和存档不变。
5. 已完成或进行中的赛事继续使用 `teamSnapshots`，不得因职业库替换而改变历史阵容或比赛结果。

迁移后启动应用并保存数据库，使当前本机 SQLite 与新职业库一致。

## 导出名单

重新生成以下文件：

- `职业战队与队员名单.csv`
- `职业战队与队员名单.md`
- `全部战队与队员名单.csv`
- `全部战队与队员名单.md`

虚构和自建名单保持原内容。全部名单应包含：

- 职业：215 队、1,032 人，其中 180 队可参赛、35 队阵容不完整；
- 虚构：50 队、250 人；
- 自建：1 队、5 人；
- 合计：266 队、1,287 人。

最终重新创建“最终所有名单”导出文件夹和 ZIP，旧的 16 队导出包不作为最终交付。

## EXE 与在线更新

重建 Windows EXE，使新安装默认拥有完整排名职业库。队伍管理页保留全部 215 支职业队；创建赛事时仍通过 `validateRoster` 只显示 180 支完整队伍。现有无浏览器 HLTV 更新器的 Cloudflare 兼容问题不在本次范围内；界面继续保留更新入口，但失败时不得破坏当前职业库。本次交付的数据更新通过已验证的浏览器会话生成静态快照，不将浏览器依赖引入应用运行时。

## 测试与验收

自动测试至少覆盖：

- 内置职业库为 215 队、1,032 名唯一选手；
- 180 支队伍恰好五名首发，35 支队伍少于五人，合计缺少 43 个位置；
- 所有阵容成员唯一且引用存在，同一选手不跨队重复；
- 不完整职业队能通过数据库读取校验，但不能进入赛事选择；
- 虚构队和自建队仍必须是完整五人阵容；
- JSON 快照日期、来源和排名连续；
- 旧 16 队数据库升级到完整职业库；
- 已自行更新的职业库不被迁移覆盖；
- 存档中的职业队快照在迁移前后字节等价；
- 导出 CSV 的队伍数、人数和非游戏 ID 字段完整；
- TypeScript、前端生产构建、Rust 测试和 Rust release 构建通过；
- 当前 SQLite 显示 215 支职业队和 1,032 名职业选手；
- 最终 EXE、源码、数据库导出和 GitHub `main` 保持一致。

## 非目标

- 不修复 Cloudflare 下的后台自动抓取。
- 不逐个抓取 1,032 个个人主页补年龄或位置。
- 不用占位选手、历史队员或 Stats 中的旧队员补齐 43 个空缺位置。
- 不为职业选手编造角色或修改官方游戏 ID。
- 不改动赛事引擎、赛制模板或界面布局。
